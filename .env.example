# 複製本檔為 .env.local 後填入真實值（.env.local 已被 .gitignore 排除，不會進 git）。

# === 後端機密：Gemini AI ===
# 只在 server.ts 使用，不會送到瀏覽器端。
# 從 https://aistudio.google.com/apikey 取得。
GEMINI_API_KEY="你的_GEMINI_API_KEY"

# === 前端：Google OAuth Client ID（Gmail 收件登入用）===
# 這是公開值（會出現在瀏覽器端），但仍以環境變數管理避免寫死。
# 取得方式：Google Cloud Console → API 與服務 → 憑證 → 建立「OAuth 用戶端 ID」→ 類型選「網頁應用程式」。
# 「已授權的 JavaScript 來源」需加入：http://localhost:3000
VITE_GOOGLE_CLIENT_ID="你的_CLIENT_ID.apps.googleusercontent.com"
