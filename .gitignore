node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# 本機成績資料庫（含學生個資/成績，不應進 git）
db.json
db.json.tmp
db.manual-backup-*.json
backups/

# 本機設定（Gemini API key / 模型選擇，含機密，不應進 git）
edugrade-config.json

# Gmail 離線暫存（信件 manifest + 下載的附件檔，含個資，不應進 git）
gmail_cache/

# 出題 .docx 匯出樣本/測試輸出
_sample_docx/

# Electron 打包輸出（安裝檔/未壓縮 app）
release/
